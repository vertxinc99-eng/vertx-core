# VERTX CORE v7.14 RETURN ORDER MATCH

- 返却重量計算の資材選択を、資材注文画面と同じグループ順・同じ資材順に統一。
- iPhoneの選択欄でも「枠足場用 / 単管足場用 / クランプ類 / …」の見出し付きで選べるよう optgroup 化。
- 各グループ内のサイズ順も注文画面と同じ materialSort を共用。
- 既存の返却車手配・重量計算・資材名カスタム機能は維持。
